# Copiá este archivo como `.env.local` y completá con tus valores reales.
# `.env.local` está en `.gitignore` — no lo subas a GitHub.
#
# Modo dummy: podés dejar vacío o comentado; la app usa datos mock y no llama a Supabase.
# En Vercel: Project Settings → Environment Variables → Production / Preview / Development.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
